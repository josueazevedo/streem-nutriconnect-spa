import { PreventSpacesDirective } from './prevent-spaces.directive';

describe('PreventSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
